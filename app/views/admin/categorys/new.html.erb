<%= form_for(@category,url: admin_categorys_path) do |f| %>

    <h4>アイコン</h4>
    <%= f.attachment_field :icon %>

    <h4>説明</h4>
    <%= f.text_field :explanation %>

      <div class="actions">
      <%= f.submit "登録" %>
    </div>
  <% end %>
